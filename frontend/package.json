{
  "name": "streamonic-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && VITE_API_URL=$(grep VITE_API_URL frontend/.env | cut -d '=' -f2) && sed -i '' 's|__API_BASE_URL__|'\"$VITE_API_URL\"'|g' dist/js/app.js"
  }
}