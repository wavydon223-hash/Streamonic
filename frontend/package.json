{
  "name": "streamonic-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "VITE_API_URL=$(grep VITE_API_URL .env | cut -d '=' -f2) && sed -i '' 's|__API_BASE_URL__|'\"$VITE_API_URL\"'|g' public/js/app.js && mkdir -p dist && cp -r public/* dist/"
  }
}